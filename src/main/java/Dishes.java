import java.util.Scanner;
public class Dishes {
    String dishList;
    double dishSum;
    Scanner sc = new Scanner(System.in);

    public void main(String[] args) {
        void (String dishName,double dishPrice);
        {
            String dishList = new dishName + "\n" + sc.nextLine();
            return dishList;
            double dishSum = new dishPrice + sc.nextDouble();
            return dishSum;
            System.out.println("Список блюд: " + dishList);
            System.out.println("Итоговый счёт: " + dishSum);
            System.out.println("Итоговая сумма с каждого гостя: " + dishSum / guestsNum + "рублей");
        }
    }
}
